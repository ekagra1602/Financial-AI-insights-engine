import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qualcomm.financialinsights',
  appName: 'Finance Dashboard',
  webDir: 'dist'
};

export default config;
